# Phase 6 — Storefront

## Pages

`/`, `/shop`, `/category/:slug`, `/product/:slug`, `/search`, `/cart`, `/checkout`, `/payment/callback`, `/account`, `/page/:slug`, 404

## UX

RTL, Vazirmatn, cart from right, گندم گالری branding, Persian copy only

## Acceptance

Browse → OTP → Digipay → order history E2E
