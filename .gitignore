# Graphify (commit graphify-out/ for team; exclude local-only artifacts)
graphify-out/cost.json
graphify-out/cache/

# Local / secret files — never commit
.env
.env.local
.env.production
backend/.env
backend/.env.production
frontend/.env
backoffice/.env
liara
*.pem
*.key
credentials.json
