{
  "collectionName": "components_sections_product_rows",
  "info": {
    "displayName": "Product Row (Grid)",
    "icon": "grid"
  },
  "options": {},
  "attributes": {
    "title": { "type": "string" },
    "link": { "type": "string" },
    "source": {
      "type": "enumeration",
      "enum": ["newest", "sale", "category", "manual"],
      "default": "newest"
    },
    "categorySlug": { "type": "string" },
    "productIds": { "type": "text" },
    "limit": { "type": "integer", "default": 8 }
  }
}
